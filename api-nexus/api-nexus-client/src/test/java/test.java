/*

import org.junit.Test;
import org.pantherslabs.chimera.unisca.api_nexus.api_nexus_client.consumer.ApiConsumer;
import org.pantherslabs.chimera.unisca.metadata_api.model.DataPipelines;

import java.io.IOException;

public class TestConsumer {
    @Test
    public void testApiCall() throws IOException, InterruptedException {
        System.out.println("calling the rest API");
        ApiConsumer consumer = new ApiConsumer();
        System.out.println("calling the rest API");
        DataPipelines dataSource = consumer.get("http://localhost:9001/metadata/api/v1/dataPipelines", new TypeReference<DataPipelines>() {});
        System.out.println(dataSource);
    }
}
*/
